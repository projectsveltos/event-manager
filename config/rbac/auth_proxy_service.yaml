apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: event-manager
  name: manager-metrics-service
  namespace: projectsveltos
spec:
  ports:
  - name: metrics
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: event-manager
